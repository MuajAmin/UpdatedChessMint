package com.example.updatedchessmint

import androidx.compose.runtime.Composable

// Navigation is handled directly in MainActivity via Compose
// This file is kept as a placeholder for future navigation needs

@Composable
fun MainNavigation() {
    // The main app content is rendered directly in MainActivity.ChessMintApp()
    // No navigation graph is needed for the single-screen WebView app
}
